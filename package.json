{
  "name": "transl8",
  "version": "1.0.0",
  "description": "AI-powered video dubbing platform",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install:all": "npm install && cd packages/backend && npm install && cd ../workers && npm install && cd ../frontend && npm install && cd ../..",
    "build": "npm run build:backend",
    "build:backend": "npm run prisma:generate && npm run build --workspace=@dubbing/backend",
    "build:workers": "npm run build --workspace=@dubbing/workers",
    "build:frontend": "npm run build --workspace=@dubbing/frontend",
    "build:all": "npm run build:backend && npm run build:workers && npm run build:frontend",
    "dev:backend": "cd packages/backend && npm run dev",
    "dev:workers": "cd packages/workers && npm run dev",
    "dev:frontend": "cd packages/frontend && npm run dev",
    "start": "npm run start --workspace=@dubbing/backend",
    "start:backend": "npm run start --workspace=@dubbing/backend",
    "start:workers": "npm run start --workspace=@dubbing/workers",
    "start:frontend": "npm run start --workspace=@dubbing/frontend",
    "deploy:frontend": "cd packages/frontend && vercel --prod",
    "prisma:migrate": "npm run prisma:migrate:deploy --workspace=@dubbing/backend",
    "prisma:migrate:deploy": "npm run prisma:migrate:deploy --workspace=@dubbing/backend",
    "prisma:generate": "npm run prisma:generate --workspace=@dubbing/backend",
    "test": "echo \"Run tests in individual packages\"",
    "lint": "eslint . --ext .ts,.tsx,.js,.jsx",
    "lint:fix": "eslint . --ext .ts,.tsx,.js,.jsx --fix",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "keywords": [
    "video",
    "dubbing",
    "translation",
    "ai",
    "tts",
    "stt"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.57.1",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  }
}
