# Requirements for Pre-Flight Validation System

# Audio processing
pydub==0.25.1
numpy==1.24.3
librosa==0.10.1
soundfile==0.12.1

# Optional: Only needed if running full validation with services
# noisereduce==2.0.1
