{
  "name": "@transl8/benchmarks",
  "version": "1.0.0",
  "description": "Model quality benchmarking suite for AI video dubbing platform",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepare-datasets": "ts-node src/scripts/prepare-datasets.ts",
    "benchmark-stt": "ts-node src/scripts/benchmark-stt.ts",
    "benchmark-mt": "ts-node src/scripts/benchmark-mt.ts",
    "benchmark-tts": "ts-node src/scripts/benchmark-tts.ts",
    "benchmark-lipsync": "ts-node src/scripts/benchmark-lipsync.ts",
    "generate-report": "ts-node src/scripts/generate-report.ts"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "csv-parser": "^3.0.0",
    "csv-writer": "^1.6.0",
    "fast-levenshtein": "^3.0.0",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
